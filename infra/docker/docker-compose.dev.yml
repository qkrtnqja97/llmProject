version: "3.8"

services:
  backend:
    build:
      context: ../../backend
    container_name: backend
    # env_file:
    #   - ../../backend/.env
    environment:
      ML_BASE_URL: http://llm:9000
      #DB_HOST: mysql
    ports:
      - "8000:8000"
    volumes:
      - ../../backend:/backendWorkspace
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload # dev 환경에서만 프로세스 재시작할래.
    depends_on:
      - llm
      # - mysql

  llm:
    build:
      context: ../../llmServer
    container_name: llm
    ports:
      - "9000:9000"
    volumes:
      - ../../llmServer:/llmWorkspace
    command: uvicorn app.api.test.test_main:app --host 0.0.0.0 --port 9000 --reload

  # mysql:
  #   image: mysql:8.0
  #   container_name: mysql
  #   env_file:
  #     - ./backend/.env
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - mysql_data:/var/lib/mysql

# volumes:
#   mysql_data:
